import React, { Component } from 'react';
import PropTypes from 'prop-types';
class TodoItem extends Component {
    static propTypes={
        name: PropTypes.string,
        id : PropTypes.string


    };


    
    render() {
        return (
            <div>import PropTypes from 'prop-types';
                

            </div>
        );
    }
}

export default TodoItem;